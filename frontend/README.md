# Cutaway — frontend

React + Vite client for the Cutaway clip finder. Paste a long-form transcript,
pick how many clips you want, and it calls the FastAPI backend to surface the
strongest moments for short-form.

## Run

```
npm install
npm run dev
```

Expects the backend at `http://127.0.0.1:8000` (see `../backend`).
